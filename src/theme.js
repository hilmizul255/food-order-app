import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: 'rgba(255, 255, 255, 1)',
    },
    secondary: {
      main: 'rgba(23, 207, 151, 1)',
    },
  },
});

export default theme;
